项目运行状态 - 2026-05-05
=====================

服务状态:
  Airflow Webserver   运行中    http://localhost:8080
  Airflow Scheduler   运行中    -
  Airflow Worker      运行中    -
  PostgreSQL          运行中    localhost:5432
  Redis               运行中    -
  SeaTunnel           运行中    localhost:5801

CI/CD 状态:
  GitHub Actions      通过      https://github.com/Zhuyuxuan0923/ShuShu/actions
  Build Badge         绿色      README.md 顶部

访问方式:
  URL:      http://localhost:8080
  用户名:   admin
  密码:     admin

组件版本:
  dbt-core            1.9.4
  dbt-postgres        1.9.1
  Apache SeaTunnel    2.3.12
  Apache Airflow      2.11.1
  PostgreSQL          16
  Redis               7-alpine

已修复的问题:
  1. Dockerfile.seatunnel: 添加 useradd 创建 seatunnel 用户
  2. Dockerfile.airflow: dbt 改为容器启动时安装，避免 pip 依赖解析超时
  3. requirements.txt: 更新 requests/psycopg2-binary 到兼容版本
  4. docker-compose.yml: 修复 SeaTunnel config 挂载路径和启动命令
  5. scripts/install_dbt.sh: 新增 dbt 启动安装脚本
  6. data_warehouse_full_pipeline.py: python_callable 从字符串改为函数引用
  7. 两个 DAG 的 schedule_interval → schedule（适配 Airflow 2.11.1）
  8. dbt-postgres 版本修正: 1.9.4 → 1.9.1（版本号独立）
  9. generate_schema_name 宏: 解决 schema 前缀问题（public_dws → dws）
  10. PostgreSQL QUALIFY 不支持: 所有 DWD 模型改用 DISTINCT ON 去重
  11. ROUND(double precision) 类型错误: 加 ::NUMERIC 转换
  12. packages.yml 删除: dbt-utils 无引用，避免 dbt deps 强制依赖
  13. 行数波动监控: 新增 row_count_monitor 插件，四层 COUNT 对比昨日快照，>30% 企微/钉钉告警
  14. Mermaid 架构图: README 新增全景流程图，GitHub 直接渲染
  15. 告警截图引导: README 中已添加，配置真实 webhook URL 即可截图

新增功能:
  - 行数波动监控: DAG 新增 monitor_row_count 任务（数据管道自愈第一步：异常检测）
  - Mermaid 架构全景图: 数据源→摄入→存储→建模→质量→调度→告警→CI/CD

已知限制:
  - Docker Hub 镜像拉取需配置 registry mirror（已在 Docker Desktop Settings 中配置）
  - GitHub API 未认证限 60次/小时，单源 DAG 每30分钟触发刚好不超限
